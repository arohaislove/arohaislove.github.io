name = "vox-api"
main = "index.js"
compatibility_date = "2024-01-01"

# Workers are deployed to *.workers.dev subdomain
# This will be available at: https://vox-api.zammel.workers.dev

# Environment variables (values set in Cloudflare dashboard and GitHub secrets)
[vars]
# ANTHROPIC_API_KEY is set as a secret, not listed here
# ELEVENLABS_API_KEY is set as a secret, not listed here
